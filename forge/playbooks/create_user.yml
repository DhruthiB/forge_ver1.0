---

  - hosts: localhost
    become: yes
    become_user: root
    tasks:
    - name: Create user
      user:
        name: '{{ stylo_user }}'
        generate_ssh_key: yes

    - name: Set home folder perms
      file:
        path: '{{ user_directory }}'
        mode: 'o+rx'
        owner: '{{ stylo_user }}'
        group: '{{ stylo_user }}'
        recurse: yes

    - name: Set /tmp/.forge folder perms
      file:
        path: '{{ repo_path }}'
        owner: '{{ stylo_user }}'
        group: '{{ stylo_user }}'
        recurse: yes

    - name: Change default shell to bash
      shell: "chsh {{ stylo_user }} -s $(which bash)"
...
