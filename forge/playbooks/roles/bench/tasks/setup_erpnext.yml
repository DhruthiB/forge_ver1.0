---
  - name: Check if ERPNext App exists
    stat: path="{{ forge_path }}/apps/erpnext"
    register: app

  - name: Get the ERPNext app
    command: forge get-app erpnext {{ erpnext_repo_url }} --branch {{ erpnext_branch }}
    args:
      creates: "{{ forge_path }}/apps/erpnext"
      chdir: "{{ forge_path }}"
    when: not app.stat.exists and not without_erpnext

  - name: Check whether the site already exists
    stat: path="{{ forge_path }}/sites/{{ site }}"
    register: site_folder
    when: not without_site

  - name: Create a new site
    command: "forge new-site {{ site }} --admin-password '{{ admin_password }}' --mariadb-root-password '{{ mysql_root_password }}'"
    args:
      chdir: "{{ forge_path }}"
    when: not without_site and not site_folder.stat.exists

  - name: Install ERPNext to default site
    command: "forge --site {{ site }} install-app erpnext"
    args:
      chdir: "{{ forge_path }}"
    when: not without_site and not without_erpnext
...